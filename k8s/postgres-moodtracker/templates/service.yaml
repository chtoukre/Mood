apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: postgres
spec:
  type: {{ .Values.service.type }}
  selector:
    app: postgres
  ports:
    - name: pg
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.postgres.port }}

